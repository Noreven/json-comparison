[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"
[project]
name = "json-diff"
version = "1.0.0"

[project.scripts]
json_diff = "json_diff.__main__:main"
